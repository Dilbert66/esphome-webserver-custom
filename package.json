{
  "name": "esphome-webserver",
  "version": "3.0.0",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:packages && npm run deploy",
    "build:packages": "npm run build --workspaces --if-present",
    "deploy": "npm run deploy --workspaces --if-present"
  },
  "dependencies": {
    "crypto-es": "^2.1.0"
  }
}
